[package]
name = "xtask"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive", "env"] }
semver = "1"
toml_edit = "0.23"

[dev-dependencies]
tempfile = "3"
